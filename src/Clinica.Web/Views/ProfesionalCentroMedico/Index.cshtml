@model IEnumerable<Clinica.Web.Models.ProfesionalCentroMedico>

@{
    ViewData["Title"] = "Index";
}

<h2>Listado</h2>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaRegistro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProfesionalCentroMedicoId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.ProfesionalCentroMedicoId">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.ProfesionalCentroMedicoId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
