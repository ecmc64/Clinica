
@model IEnumerable<Clinica.Web.Models.Profesional>

@{
    ViewData["Title"] = "Index";
}

<h2>Listado</h2>



<form asp-action="Exportar">
    <p>
        <a asp-action="Create">Crear Nuevo</a> | 
        @*<div class="form-actions no-color">*@
        <button type="submit" class="btn btn-success" formaction=@Url.Action("Exportar") formmethod="post">
            Exportar Excel
        </button>
        @*</div>*@
    </p>
</form>

<div id="grid"></div>

@section StylesHead{
    @{await Html.RenderPartialAsync("_ViewImportsKendoStyles");}
}

@section scripts{
    @{ await Html.RenderPartialAsync("_ViewImportsKendoScripts");}
    <script src="~/js/profesionales/index.js"></script>
}